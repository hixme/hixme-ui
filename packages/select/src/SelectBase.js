import styled from 'styled-components'
import { InputBase } from '@hixme-ui/input'

export default styled(InputBase.withComponent('select'))`
  box-shadow: '0px 1px 3px 0px rgba(185,197,205,0.50)';
`
